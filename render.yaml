services:
  - type: web
    name: seo-keyword-intel-backend
    env: python
    plan: free
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "gunicorn backend.main:app --bind 0.0.0.0:$PORT --workers 2"
    envVars:
      - key: PORT
        value: "10000"
