# SEO Keyword Intelligence Tool

A small production-ready Python backend + static frontend that scrapes Google SERPs for seed keywords, builds TF-IDF vectors, clusters with K-means, and exports results as CSV/JSON. Includes a tiny frontend to run jobs and download exports.

## Repo layout

